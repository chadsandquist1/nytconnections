This application has a title screen at index.html.  It is the totle for a series of react based apps.

Most of the underlying apps will be standalone react apps.

Whenever you add an new folder, please always adjust the title screen accordingly.

When you are done, always sync the s3 bucket and if applicable, re-run terraform apply if required.  

NEVER check in any .aws credentials, terraform statefiles, or secret keys.

Use modern quality practices.
